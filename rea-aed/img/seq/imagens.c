
	int x, y, num;
	float K, Z, CONS;
	char x1, x2, x3;
	
	
	
			x = 4;
			y = x + 3;
			z = 2.5;
			id = 'F';
	
	
	
	scanf("Expressão de controle", lista de argumentos);
	scanf("%d", &x);
	scanf("%f", &y);
	scanf("%d %f", &x, &y);
	
	
	
	printf("%d", x);
	printf("Valor de x: %d", x);
	
	
	#include <stdio.h>
	/*função main inicia a execução do programa*/
	int main(){
		
		blocos de comando;
		
		return 0;/*valor caso o programa seja executado com sucesso*/
	}
	
	
	
	
	#include <stdio.h>
	/*função main inicia a execução do programa*/
	int main(){
		/*declaração das variáveis*/
		int n1, n2 , n3;
		float resultado;
		scanf("%d", &n1); /*comando de entrada, será digitado o valor de n1*/
		scanf("%d", &n2); /*comando de entrada, será digitado o valor de n2*/
		n3 = 20; /*comando de atribuição, n3 recebe o valor 20*/
		resultado = (n1 + n2 +n3) / 3; /*resultado recebe o valor da soma n1+n2+n3 dividido por 3,
														 a soma esta entre parênteses devido às regras de precedência,
														pois é necessário fazer primeiramente a soma para em seguida dividir*/
		printf("Valor da media: %d", resultado); /*Impressão do valor da média*/
		return 0;/*valor de retorno caso o programa seja executado com sucesso*/
	}
	
	
	
	
	